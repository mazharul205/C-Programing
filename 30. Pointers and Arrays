#include <stdio.h>
int main() {

  int i, x[8], sum = 0;

  printf("Enter 8 numbers: ");

  for(i = 0; i < 8; ++i) {
  
      scanf("%d", x+i);

 
      sum += *(x+i);
  }

  printf("Sum = %d", sum);

  return 0;
}
