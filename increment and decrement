#include <stdio.h>
int main()
{
    int a = 100, b = 10;
    float c = 20.5, d = 30.5;

    printf("++a = %d \n", ++a);
    printf("--b = %d \n", --b);
    printf("++c = %f \n", ++c);
    printf("--d = %f \n", --d);

    return 0;
}
